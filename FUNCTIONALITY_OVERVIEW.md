# Описание функционала системы FlashApp

Документ описывает реализованный функционал мини‑приложения Telegram для записи на услуги, администрирования заказов и управления контентом (услуги/цены/галерея) с хранением данных в Google Sheets.

---

## 1) Общая архитектура

- **Клиент (Frontend)**: React + TypeScript + Zustand (состояние) + Framer Motion (анимации).
- **Сервер (Backend)**: Node.js + Express.
- **Хранилище данных**: Google Sheets (несколько листов: услуги, заказы, админы, галерея).
- **Интеграция с Telegram**:
  - Мини‑приложение запускается внутри Telegram.
  - Уведомления через бота (Telegraf) при создании заказа и изменении статуса.

---

## 2) Пользовательская часть (клиент)

### 2.1 Просмотр услуг
- Пользователь видит список услуг, сгруппированных по категориям.
- Карточка услуги показывает базовую информацию (название, описание, иконка) и ориентир по цене.

### 2.2 Галерея работ
- Для каждой услуги доступна галерея фото.
- Возможности:
  - Просмотр в полноэкранном режиме.
  - Переключение фото (кнопки/клавиатура).
  - Превью‑миниатюры.

### 2.3 Оформление заказа (BookingFlow)
- Пошаговый сценарий оформления:
  - Выбор услуги.
  - Выбор класса автомобиля.
  - Выбор даты и времени.
  - Ввод контактных данных.
- **Расчет цены**:
  - Цена зависит от выбранной услуги и класса автомобиля.
  - Итоговая стоимость отображается пользователю до отправки.

### 2.4 Поддержка нескольких услуг в одном заказе
- На уровне модели заказа предусмотрен массив услуг `services`.
- В текущем пользовательском UI выбор обычно начинается с одной услуги; добавление дополнительных услуг возможно через админ‑панель.

---

## 3) Админ‑панель

Админ‑панель доступна только пользователям, добавленным в лист `admins` в Google Sheets.

### 3.1 Управление заказами
- Просмотр всех заказов.
- Фильтрация по статусам (например: новый / в работе / готов).
- Смена статуса заказа.
- Отображение ключевых параметров:
  - услуги (в т.ч. несколько услуг в одном заказе)
  - класс авто
  - дата/время
  - телефон
  - итоговая цена

#### Редактирование заказа
- Доступно редактирование полей заказа без захода в таблицу:
  - список услуг (добавить/удалить)
  - дата
  - время
  - телефон
  - класс автомобиля
- Цена пересчитывается автоматически при изменении списка услуг.

#### Удаление заказа
- Заказ можно удалить из админ‑панели (с подтверждением).

### 3.2 Управление услугами (ServiceManager)
- CRUD для услуг:
  - создание новой услуги
  - редактирование существующей
  - удаление услуги
- Управление ценами:
  - цены на каждый класс авто: economy / comfort / business / premium

### 3.3 Управление галереей услуги
- Для каждой услуги администратор может:
  - добавлять фото в галерею (по URL)
  - задавать описание
  - удалять фото

### 3.4 Управление администраторами
- Добавление/удаление администраторов через UI.
- Данные хранятся в Google Sheets (лист `admins`).

---

## 4) Google Sheets: структура данных

### 4.1 Лист `services`
Хранит список услуг и цены.

Рекомендуемые поля:
- `id`
- `name`
- `description`
- `basePrice`
- `economyPrice`
- `comfortPrice`
- `businessPrice`
- `premiumPrice`
- `category`
- `image` (emoji/иконка)
- `active`

### 4.2 Лист `orders`
Хранит заказы пользователей.

Рекомендуемые поля:
- `id`
- `userId`
- `userName`
- `services` (JSON массив услуг)
- `carClass`
- `date`
- `time`
- `phone`
- `price` (итоговая стоимость)
- `status`
- `createdAt`

> Примечание: для совместимости может присутствовать старое поле `service` (одна услуга).

### 4.3 Лист `gallery`
Хранит фото работ по услугам.

Рекомендуемые поля:
- `id`
- `serviceId`
- `imageUrl`
- `description`
- `order` (порядок вывода)

### 4.4 Лист `admins`
Хранит список администраторов.

Рекомендуемые поля:
- `userId`
- `name`

---

## 5) API (основные возможности)

### 5.1 Services
- `GET /api/services` — получить список услуг.
- `POST /api/services` — создать/обновить услугу.
- `DELETE /api/services/:serviceId` — удалить услугу.

### 5.2 Gallery
- `GET /api/gallery/:serviceId` — получить фото услуги.
- `POST /api/gallery/:serviceId` — добавить фото.
- `DELETE /api/gallery/:serviceId/:imageId` — удалить фото.

### 5.3 Orders
- `GET /api/orders` — получить заказы.
- `POST /api/orders` — создать заказ.
- `PATCH /api/orders/:orderId/status` — сменить статус.
- `PATCH /api/orders/:orderId` — обновить заказ (редактирование).
- `DELETE /api/orders/:orderId` — удалить заказ.

### 5.4 Admins
- `GET /api/admins` — получить список админов.
- `POST /api/admins` — добавить админа.
- `DELETE /api/admins/:userId` — удалить админа.

---

## 6) Telegram‑уведомления

- При создании нового заказа администратор получает уведомление.
- При изменении статуса заказа пользователь получает уведомление.
- В уведомлениях используются ссылки/кнопки для перехода в мини‑приложение.

---

## 7) Заполнение тестовыми данными

В проекте предусмотрены скрипты для заполнения Google Sheets тестовыми услугами и галереей:
- `npm run seed:services`
- `npm run seed:gallery`
- `npm run seed:all`

---

## 8) Загрузка фото в галерею

### Способ 1: Через Google Sheets
Добавьте записи в лист `gallery`:
- `id` - уникальный идентификатор (IMG-001, IMG-002...)
- `serviceId` - ID услуги из листа `services`
- `imageUrl` - прямая ссылка на изображение
- `description` - описание (опционально)
- `order` - порядок отображения (1, 2, 3...)

**Где взять ссылки:**
- **Imgur** (рекомендуется): https://imgur.com
- **Google Drive**: используйте формат `https://drive.google.com/uc?export=view&id=FILE_ID`
- **Cloudinary**: https://cloudinary.com

### Способ 2: Через админ-панель
1. Откройте вкладку "Услуги"
2. Нажмите на иконку 🖼️ у нужной услуги
3. Вставьте URL изображения и нажмите "Добавить фото"

**Важно:** Ссылка должна быть прямой (заканчиваться на .jpg, .png, .webp) и публично доступной.

Подробная инструкция: `GALLERY_SETUP.md`

---

## 9) Ограничения и допущения

- Фото в галерее добавляются как ссылки (URL). Загрузка файлов на сервер не реализована.
- Пользовательский UI обычно формирует заказ с одной услугой; расширение на выбор нескольких услуг пользователем возможно отдельным этапом.
