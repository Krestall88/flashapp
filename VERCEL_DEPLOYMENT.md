# 🚀 Деплой на Vercel - Полная инструкция

## ✅ Что сделано

Проект **flashapp** теперь полностью готов к деплою на Vercel как единое приложение (клиент + сервер).

### Изменения в структуре:

1. **Создана папка `/api`** - serverless функции для Vercel
   - `api/index.js` - главный API endpoint
   - `api/bot/webhook.js` - Telegram bot webhook
   - `api/services/sheetsService.js` - работа с Google Sheets

2. **Создан `vercel.json`** - конфигурация для Vercel
   - Настроены rewrites для API
   - Настроены CORS headers
   - Настроена маршрутизация

3. **Добавлены новые функции:**
   - **CompanySettings.tsx** - управление настройками компании в админке
   - **ContactModal.tsx** - модальное окно для связи с компанией
   - **HomeView.tsx** - обновлен для отображения информации о компании

4. **Обновлен `package.json`** - добавлены зависимости для serverless функций

---

## 📋 Подготовка к деплою

### 1. Установите зависимости

```bash
cd c:\Users\Тайм\Documents\flashapp
npm install
```

### 2. Проверьте переменные окружения

Убедитесь, что у вас есть файл `.env` с:

```env
# Telegram
BOT_TOKEN=ваш_токен_бота
TELEGRAM_BOT_TOKEN=ваш_токен_бота
VITE_ADMIN_ID=ваш_telegram_id
ADMIN_ID=ваш_telegram_id

# Google Sheets
GOOGLE_SHEET_ID=id_таблицы
GOOGLE_SERVICE_ACCOUNT_EMAIL=email@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# URLs
VITE_API_URL=https://ваш-домен.vercel.app
WEB_APP_URL=https://ваш-домен.vercel.app

# Port (для локальной разработки)
PORT=5000
```

---

## 🌐 Деплой на Vercel

### Шаг 1: Установите Vercel CLI (опционально)

```bash
npm install -g vercel
```

### Шаг 2: Войдите в Vercel

```bash
vercel login
```

### Шаг 3: Деплой проекта

```bash
vercel
```

Или через GitHub:
1. Загрузите проект на GitHub
2. Подключите репозиторий к Vercel через dashboard
3. Vercel автоматически задеплоит проект

### Шаг 4: Настройте переменные окружения в Vercel

В Vercel Dashboard → Settings → Environment Variables добавьте:

```
BOT_TOKEN=ваш_токен
TELEGRAM_BOT_TOKEN=ваш_токен
VITE_ADMIN_ID=ваш_id
ADMIN_ID=ваш_id
GOOGLE_SHEET_ID=id_таблицы
GOOGLE_SERVICE_ACCOUNT_EMAIL=email@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY=ваш_приватный_ключ
VITE_API_URL=https://ваш-домен.vercel.app
WEB_APP_URL=https://ваш-домен.vercel.app
```

**ВАЖНО:** Для `GOOGLE_PRIVATE_KEY` скопируйте весь ключ включая `-----BEGIN PRIVATE KEY-----` и `-----END PRIVATE KEY-----`

### Шаг 5: Настройте Telegram Bot Webhook

После деплоя, установите webhook для бота:

```bash
curl -X POST "https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook?url=https://ваш-домен.vercel.app/api"
```

Или используйте скрипт:

```bash
node api/bot/set-webhook.js
```

---

## 🔧 Структура проекта для Vercel

```
flashapp/
├── api/                          # Serverless функции
│   ├── index.js                  # Главный API endpoint
│   ├── bot/
│   │   └── webhook.js            # Telegram bot
│   └── services/
│       └── sheetsService.js      # Google Sheets
├── src/                          # Frontend (React + Vite)
│   ├── components/
│   │   ├── ContactModal.tsx      # NEW: Модальное окно контактов
│   │   └── views/
│   │       ├── CompanySettings.tsx  # NEW: Настройки компании
│   │       ├── HomeView.tsx      # UPDATED: С информацией о компании
│   │       └── AdminDashboard.tsx   # UPDATED: С вкладкой Settings
│   └── api/
│       └── client.ts             # UPDATED: Добавлены методы settings
├── vercel.json                   # NEW: Конфигурация Vercel
└── package.json                  # UPDATED: Добавлены зависимости
```

---

## 🎯 Новый функционал

### 1. Настройки компании (Админка)

Администраторы могут настроить:
- Название компании
- Адрес
- Телефон
- Email
- Сайт
- Описание
- Режим работы (часы и дни)
- Способы связи (телефон, WhatsApp, Telegram)

**Как использовать:**
1. Откройте админку
2. Перейдите на вкладку "Настройки"
3. Заполните информацию
4. Нажмите "Сохранить изменения"

### 2. Информация о компании на главной

Пользователи видят на главном экране:
- Название компании
- Описание
- Адрес
- Кнопку "Связаться с нами"
- Ссылку на сайт
- Режим работы

### 3. Модальное окно контактов

При нажатии "Связаться с нами" открывается модальное окно с:
- Номером телефона
- Кнопками для звонка
- Кнопками для WhatsApp (если включено)
- Кнопками для Telegram (если включено)

---

## 📊 API Endpoints

Все API endpoints доступны через `/api`:

### Orders
- `GET /api/orders` - Получить все заказы
- `POST /api/orders` - Создать заказ
- `PATCH /api/orders/:id` - Обновить заказ
- `PATCH /api/orders/:id/status` - Обновить статус
- `DELETE /api/orders/:id` - Удалить заказ

### Services
- `GET /api/services` - Получить услуги
- `POST /api/services` - Создать/обновить услугу
- `DELETE /api/services/:id` - Удалить услугу

### Settings (NEW)
- `GET /api/settings` - Получить настройки компании
- `PUT /api/settings` - Обновить настройки компании

### Clients
- `GET /api/clients` - Получить клиентов
- `GET /api/clients/:userId/orders` - Заказы клиента

### Admins
- `GET /api/admins` - Получить админов
- `POST /api/admins` - Добавить админа
- `DELETE /api/admins/:userId` - Удалить админа

### Gallery
- `GET /api/gallery?serviceId=:id` - Получить галерею
- `POST /api/gallery` - Добавить фото
- `DELETE /api/gallery/:id` - Удалить фото

### Bot Webhook
- `POST /api` - Telegram bot webhook

---

## 🧪 Тестирование

### Локальное тестирование

```bash
# Терминал 1 - Frontend
npm run dev

# Терминал 2 - Backend (для локальной разработки)
npm run server:dev
```

### Тестирование на Vercel

После деплоя:
1. Откройте `https://ваш-домен.vercel.app`
2. Проверьте главную страницу - должна отображаться информация о компании
3. Откройте админку → Настройки
4. Измените настройки и сохраните
5. Вернитесь на главную - изменения должны отобразиться

---

## 🔍 Troubleshooting

### Ошибка: "Cannot find module"
- Убедитесь, что все зависимости установлены: `npm install`
- Проверьте `package.json` - должны быть все зависимости

### Ошибка: "Google Sheets API error"
- Проверьте `GOOGLE_PRIVATE_KEY` в Vercel Environment Variables
- Убедитесь, что Service Account имеет доступ к таблице
- Проверьте формат ключа (должен включать `-----BEGIN` и `-----END`)

### Ошибка: "Bot webhook failed"
- Проверьте `BOT_TOKEN` в Vercel Environment Variables
- Убедитесь, что webhook установлен правильно
- Проверьте URL webhook: `https://ваш-домен.vercel.app/api`

### Настройки не сохраняются
- Проверьте, что в Google Sheets есть лист `settings`
- Проверьте права доступа Service Account
- Проверьте консоль браузера на ошибки

---

## 📝 Важные заметки

1. **Serverless функции** - каждый запрос к `/api` создает новый экземпляр функции
2. **Кэширование** - используется NodeCache для оптимизации запросов к Google Sheets
3. **CORS** - настроен для работы с любыми доменами (можно ограничить в `vercel.json`)
4. **Переменные окружения** - должны быть настроены и в Vercel, и локально

---

## ✅ Checklist перед деплоем

- [ ] Установлены все зависимости (`npm install`)
- [ ] Создан `.env` с правильными значениями
- [ ] Google Sheets настроены (листы: services, orders, clients, admins, gallery, settings)
- [ ] Service Account имеет доступ к таблице
- [ ] Telegram бот создан и токен получен
- [ ] Проект собирается без ошибок (`npm run build`)
- [ ] Переменные окружения добавлены в Vercel
- [ ] Webhook бота настроен на Vercel URL

---

**Готово! Проект полностью готов к деплою на Vercel! 🚀**

Теперь клиент и сервер объединены в одном проекте, и вы можете задеплоить все одной командой.
