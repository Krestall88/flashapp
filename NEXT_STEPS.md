# Следующие шаги для диагностики

## ✅ Сервер запущен успешно

Сервер работает на `http://localhost:3000`

---

## 🔍 Что нужно сделать прямо сейчас:

### 1. Откройте приложение локально

Откройте в браузере: **http://localhost:3000**

### 2. Откройте консоль браузера

Нажмите **F12** → вкладка **Console**

### 3. Перейдите в админку

В приложении перейдите в **Админ-панель → вкладка "Услуги"**

### 4. Скопируйте логи из терминала

В терминале где запущен сервер должны появиться логи типа:

```
[SERVER] 📥 API: GET /api/services
[SERVER] 🔍 Loading services from Google Sheets...
[SERVER] 📊 Total rows in services sheet: X
[SERVER] Service SRV-1: active=true, filtered=true
[SERVER] ✅ Loaded services: X
[SERVER] 📤 API: Returning X services
```

**Скопируйте эти логи и отправьте мне!**

### 5. Нажмите на иконку галереи

Если услуги отображаются, нажмите на иконку 🖼️ у любой услуги.

В терминале должны появиться логи:

```
[SERVER] 📥 API: GET /api/gallery?serviceId=1
[SERVER] 🔍 Loading gallery from Google Sheets for serviceId: 1
[SERVER] 📊 Total rows in gallery sheet: X
[SERVER] 🔍 Filtering gallery for serviceId: 1
[SERVER] Image IMG-001: serviceId="1" (1) vs "1" (1), match=true
[SERVER] ✅ Loaded gallery images: X
[SERVER] 📤 API: Returning X gallery images
```

**Скопируйте эти логи и отправьте мне!**

---

## 📊 Что покажут логи:

### Если услуги не отображаются:

Логи покажут:
- Сколько строк в таблице `services`
- Какое значение в поле `active` у каждой услуги
- Сколько услуг прошло фильтр (active=true)
- Сколько услуг вернулось клиенту

### Если галерея пустая:

Логи покажут:
- Сколько строк в таблице `gallery`
- Какие `serviceId` сравниваются
- Почему фото не проходят фильтр (match=true/false)

---

## 🎯 Возможные проблемы и что покажут логи:

### Проблема 1: В таблице `services` поле `active` не равно "true"

**Логи покажут:**
```
Service SRV-1: active=false, filtered=false
Service SRV-2: active=TRUE, filtered=false  ← должно быть "true" (текст)
Service SRV-3: active=1, filtered=false     ← должно быть "true" (текст)
```

**Решение:** Изменить в Google Sheets поле `active` на текст `true`

### Проблема 2: serviceId в галерее не совпадает с ID услуги

**Логи покажут:**
```
Image IMG-001: serviceId="SRV-1" (SRV-1) vs "1" (1), match=false
```

Это значит:
- В таблице `services` ID услуги = `"SRV-1"`
- В таблице `gallery` serviceId = `"1"`
- Они не совпадают!

**Решение:** Изменить в Google Sheets лист `gallery` - поле `serviceId` должно совпадать с `id` из листа `services`

### Проблема 3: Таблица пустая

**Логи покажут:**
```
📊 Total rows in services sheet: 0
✅ Loaded services: 0
```

**Решение:** Запустить seed-скрипт:
```bash
cd server
npm run seed:services
npm run seed:gallery
```

---

## 🚀 После получения логов

Как только вы отправите мне логи из терминала сервера, я:

1. **Точно определю проблему**
2. **Исправлю её**
3. **Протестирую локально**
4. **Задеплою на Vercel**

---

**Жду логи из терминала сервера!** 📋
