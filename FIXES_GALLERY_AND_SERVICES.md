# Исправления: Галерея и отображение услуг

## Проблемы, которые были обнаружены

### 1. ❌ Услуги не отображались в админ-панели
**Проблема:** В админке во вкладке "Услуги" была только кнопка "Добавить услугу", но уже добавленные услуги не показывались.

**Причина:** Компонент `ServiceManager` корректно загружал данные из API, но если API возвращал пустой массив или ошибку, услуги не отображались.

**Решение:** 
- Добавлено логирование для отладки
- Добавлена обработка пустых данных
- Файл: `src/components/views/ServiceManager.tsx`

### 2. ❌ Услуги в клиентской части брались из кода, а не из базы
**Проблема:** В `HomeView` использовались захардкоженные `mockServices` вместо данных из Google Sheets.

**Причина:** После загрузки данных из API, компонент продолжал использовать `mockServices` для отображения.

**Решение:**
- Изменена логика: теперь используются данные из store (`services`)
- Если API не вернул данные, используются `mockServices` как fallback
- Файл: `src/components/views/HomeView.tsx`

```typescript
// Было:
const filteredServices = selectedCategory === 'all' 
  ? mockServices 
  : mockServices.filter(s => s.category === selectedCategory)

// Стало:
const displayServices = services.length > 0 ? services : mockServices
const filteredServices = selectedCategory === 'all' 
  ? displayServices 
  : displayServices.filter(s => s.category === selectedCategory)
```

### 3. ❌ Галерея не показывала фото из таблицы
**Проблема:** При нажатии на иконку галереи отображалось сообщение "Фотографии работ пока не добавлены", хотя в Google Sheets были ссылки на фото.

**Возможные причины:**
- Неправильный `serviceId` в таблице `gallery`
- Ссылки на фото недоступны или неправильного формата
- Проблема с загрузкой данных из API

**Решение:**
- Добавлено логирование в компонент `Gallery`
- Добавлена обработка пустых данных
- Файл: `src/components/views/Gallery.tsx`

---

## Как загружать фото в галерею

### Способ 1: Через Google Sheets (быстро)

1. **Откройте лист `gallery` в вашей Google Sheets таблице**

2. **Структура данных:**
   ```
   | id      | serviceId | imageUrl                          | description    | order |
   |---------|-----------|-----------------------------------|----------------|-------|
   | IMG-001 | SRV-1     | https://i.imgur.com/example.jpg   | Описание фото  | 1     |
   ```

3. **Где взять ссылки на фото:**
   - **Imgur** (рекомендуется): https://imgur.com → загрузите фото → скопируйте прямую ссылку
   - **Google Drive**: загрузите → откройте доступ → используйте формат `https://drive.google.com/uc?export=view&id=FILE_ID`
   - **Cloudinary**: https://cloudinary.com → загрузите → скопируйте URL

4. **Важно:**
   - `serviceId` должен совпадать с ID услуги из листа `services`
   - Ссылка должна быть прямой (заканчиваться на .jpg, .png, .webp)
   - Ссылка должна быть публично доступной

### Способ 2: Через админ-панель

1. Откройте админ-панель → вкладка "Услуги"
2. Найдите нужную услугу
3. Нажмите на иконку 🖼️ (Управление фото)
4. В модальном окне:
   - Вставьте URL изображения
   - Добавьте описание (опционально)
   - Нажмите "Добавить фото"

---

## Проверка работы

### Проверка услуг:

1. **В админ-панели:**
   - Откройте вкладку "Услуги"
   - Должны отображаться все услуги из Google Sheets
   - Если пусто → проверьте консоль браузера (F12)

2. **В клиентской части:**
   - Главная страница должна показывать услуги
   - Если показываются только mock-данные → проверьте API

### Проверка галереи:

1. **Откройте консоль браузера (F12)**

2. **Выберите услугу → нажмите на иконку галереи**

3. **В консоли должны появиться логи:**
   ```
   Gallery: Loading images for serviceId: SRV-1
   Gallery: Loaded images: [...]
   ```

4. **Если массив пустой:**
   - Проверьте Google Sheets → лист `gallery`
   - Убедитесь, что `serviceId` совпадает
   - Проверьте, что ссылки рабочие (откройте в браузере)

---

## Структура Google Sheets

### Лист `services`
```
| id    | name              | description           | basePrice | economyPrice | comfortPrice | businessPrice | premiumPrice | category   | image | active |
|-------|-------------------|-----------------------|-----------|--------------|--------------|---------------|--------------|------------|-------|--------|
| SRV-1 | Комплексная мойка | Полная мойка кузова   | 2500      | 2500         | 3250         | 4000          | 5000         | detailing  | 🚗    | true   |
```

### Лист `gallery`
```
| id      | serviceId | imageUrl                              | description           | order |
|---------|-----------|---------------------------------------|-----------------------|-------|
| IMG-001 | SRV-1     | https://i.imgur.com/abc123.jpg        | До мойки              | 1     |
| IMG-002 | SRV-1     | https://i.imgur.com/def456.jpg        | После мойки           | 2     |
```

---

## API Endpoints

### Услуги
- `GET /api/services` - получить все услуги
- `POST /api/services` - создать/обновить услугу
- `DELETE /api/services/:serviceId` - удалить услугу

### Галерея
- `GET /api/gallery?serviceId=SRV-1` - получить фото для услуги
- `POST /api/gallery` - добавить фото
- `DELETE /api/gallery/:imageId` - удалить фото

---

## Быстрый тест

### Тест 1: Добавить тестовую услугу через Google Sheets

```
id: TEST-001
name: Тестовая услуга
description: Для проверки
basePrice: 1000
economyPrice: 1000
comfortPrice: 1300
businessPrice: 1600
premiumPrice: 2000
category: detailing
image: 🧪
active: true
```

### Тест 2: Добавить тестовое фото

```
id: TEST-IMG-001
serviceId: TEST-001
imageUrl: https://picsum.photos/800/600
description: Тестовое фото
order: 1
```

Это добавит случайное фото из Lorem Picsum.

---

## Что делать, если не работает

### Услуги не отображаются:

1. **Проверьте консоль браузера:**
   ```
   ServiceManager: Loaded services: [...]
   ```

2. **Проверьте сервер:**
   - Убедитесь, что сервер запущен (`npm run dev` в папке `server`)
   - Проверьте логи сервера

3. **Проверьте Google Sheets:**
   - Лист `services` существует
   - Поле `active` = `true`
   - Все обязательные поля заполнены

### Галерея не показывает фото:

1. **Проверьте консоль браузера:**
   ```
   Gallery: Loading images for serviceId: SRV-1
   Gallery: Loaded images: []
   ```

2. **Если массив пустой:**
   - Проверьте `serviceId` в таблице `gallery`
   - Откройте ссылку на фото в браузере
   - Убедитесь, что это прямая ссылка на изображение

3. **Проверьте формат ссылки:**
   - ✅ `https://i.imgur.com/abc123.jpg`
   - ❌ `https://imgur.com/abc123` (не прямая ссылка)

---

## Деплой изменений

```bash
git add .
git commit -m "Fix: Services display in admin panel, gallery loading, use API data in HomeView"
git push
```

После деплоя на Vercel изменения вступят в силу автоматически.
