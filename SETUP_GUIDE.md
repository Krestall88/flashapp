# 🚀 Пошаговая инструкция по настройке

## Шаг 1: Установка зависимостей

### Одной командой (рекомендуется)
```bash
npm run install:all
```

Эта команда установит зависимости для frontend и backend автоматически.

### Или раздельно
```bash
# Frontend
npm install

# Backend
cd server
npm install
cd ..
```

## Шаг 2: Настройка Google Sheets

### 2.1 Создание Google Cloud Project
1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите **Google Sheets API**:
   - Перейдите в "APIs & Services" > "Library"
   - Найдите "Google Sheets API"
   - Нажмите "Enable"

### 2.2 Создание Service Account
1. Перейдите в "APIs & Services" > "Credentials"
2. Нажмите "Create Credentials" > "Service Account"
3. Заполните:
   - Service account name: `telegram-detailing-bot`
   - Service account ID: автоматически
   - Нажмите "Create and Continue"
4. Пропустите опциональные шаги (Grant access, Done)
5. Найдите созданный Service Account в списке
6. Нажмите на него и перейдите в "Keys"
7. Нажмите "Add Key" > "Create new key"
8. Выберите JSON формат
9. Скачайте файл (он содержит `private_key` и `client_email`)

### 2.3 Создание Google Sheet
1. Создайте новую [Google Таблицу](https://sheets.google.com/)
2. Назовите её, например: "Telegram Detailing DB"
3. Скопируйте Sheet ID из URL:
   ```
   https://docs.google.com/spreadsheets/d/[SHEET_ID]/edit
   ```
4. Нажмите "Share" (Поделиться)
5. Добавьте email Service Account (из JSON файла: `client_email`)
6. Дайте права "Editor" (Редактор)

### 2.4 Создание листов в таблице
Создайте 3 листа (вкладки) с точными названиями:

#### Лист 1: `services`
Заголовки (первая строка):
```
id | name | description | price | category | image | active
```

Пример данных:
```
1 | Комплексная мойка | Полная мойка кузова и салона | 2500 | detailing | 🚗 | true
2 | Полировка кузова | Восстановление блеска ЛКП | 8500 | detailing | ✨ | true
```

#### Лист 2: `orders`
Заголовки (первая строка):
```
id | userId | userName | service | carClass | date | time | phone | status | createdAt
```
(Данные будут добавляться автоматически)

#### Лист 3: `settings`
Заголовки (первая строка):
```
key | efwe
```

## Шаг 3: Создание Telegram Bot

### 3.1 Создание бота
1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду: `/newbot`
3. Введите имя бота: `Detailing Service Bot`
4. Введите username: `your_detailing_bot` (должен заканчиваться на `bot`)
5. Скопируйте **токен** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 3.2 Настройка Web App
1. Отправьте @BotFather команду: `/mybots`
2. Выберите своего бота
3. Нажмите "Bot Settings" > "Menu Button"
4. Выберите "Configure menu button"
5. Введите URL вашего приложения (после деплоя)
   - Для разработки: `https://your-ngrok-url.ngrok.io`
   - Для продакшена: `https://your-domain.com`

### 3.3 Получение Admin ID
1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте ему любое сообщение
3. Скопируйте ваш **User ID** (например: `123456789`)

## Шаг 4: Настройка переменных окружения

### 4.1 Frontend `.env`
Создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:5000
VITE_ADMIN_ID=123456789
```

### 4.2 Backend `server/.env`
Создайте файл `server/.env` (используйте `server/env-template.txt` как шаблон):

```env
PORT=5000
TELEGRAM_BOT_TOKEN=your_bot_token_here
ADMIN_ID=your_telegram_admin_id
WEB_APP_URL=http://localhost:3000
GOOGLE_SHEET_ID=your_google_sheet_id
GOOGLE_SERVICE_ACCOUNT_EMAIL=your_service_account@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
```

**Важно для `WEB_APP_URL`:**
- Для разработки: `http://localhost:3000` или ngrok URL
- Для продакшена: `https://your-app.vercel.app`

**Важно:** 
- `GOOGLE_PRIVATE_KEY` должен быть в кавычках
- Переносы строк должны быть как `\n`

## Шаг 5: Запуск приложения

### Способ 1: Одной командой (рекомендуется)
```bash
npm run dev
```

Эта команда запустит **одновременно** frontend и backend с помощью `concurrently`.

Вы увидите цветной вывод:
```
[CLIENT] VITE v6.0.7  ready in XXX ms
[CLIENT] ➜  Local:   http://localhost:3000/
[SERVER] 🚀 Server running on port 5000
[SERVER] ✅ Telegram bot started
[SERVER] ✅ Google Sheets initialized
[SERVER] 📊 Google Sheets connected: [Название вашей таблицы]
```

**Преимущества:**
- ✅ Один терминал
- ✅ Цветной вывод (CLIENT - голубой, SERVER - пурпурный)
- ✅ Автоматический перезапуск при изменениях
- ✅ Ctrl+C останавливает оба сервера

### Способ 2: Раздельно (для отладки)
```bash
# Терминал 1 - Backend
npm run dev:server

# Терминал 2 - Frontend
npm run dev:client
```

Используйте этот способ, если нужно видеть логи отдельно или отлаживать один из серверов.

## Шаг 6: Тестирование

### 6.1 Локальное тестирование
1. Откройте http://localhost:3000
2. Проверьте, что страница загружается
3. Попробуйте выбрать услугу и пройти процесс бронирования

### 6.2 Тестирование в Telegram (через ngrok)
1. Установите [ngrok](https://ngrok.com/)
2. Запустите:
   ```bash
   ngrok http 3000
   ```
3. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)
4. Обновите в @BotFather Menu Button URL на ngrok URL
5. Откройте бота в Telegram и нажмите кнопку меню

### 6.3 Проверка Admin панели
1. Убедитесь, что `VITE_ADMIN_ID` совпадает с вашим Telegram User ID
2. В приложении должна появиться вкладка "Админ"
3. Проверьте отображение заказов

## Шаг 7: Деплой (Production)

### Frontend (Vercel)
1. Установите Vercel CLI: `npm i -g vercel`
2. Запустите: `vercel`
3. Следуйте инструкциям
4. Добавьте Environment Variables в Vercel Dashboard:
   - `VITE_API_URL` - URL вашего backend
   - `VITE_ADMIN_ID` - ваш Telegram ID

### Backend (Railway/Render)
1. Создайте аккаунт на [Railway](https://railway.app/) или [Render](https://render.com/)
2. Подключите GitHub репозиторий
3. Укажите:
   - Build Command: `cd server && npm install`
   - Start Command: `cd server && npm start`
4. Добавьте все Environment Variables из `server/.env`

### Обновление Bot Menu URL
1. После деплоя обновите URL в @BotFather
2. Используйте production URL вместо ngrok

## 🔧 Troubleshooting

### Ошибка: "Cannot find module"
```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install

cd server
rm -rf node_modules package-lock.json
npm install
```

### Ошибка: "Google Sheets authentication failed"
- Проверьте, что Service Account email добавлен в Google Sheet
- Проверьте формат `GOOGLE_PRIVATE_KEY` (должны быть `\n`)
- Убедитесь, что Google Sheets API включен в проекте

### Ошибка: "Telegram bot failed to start"
- Проверьте правильность `TELEGRAM_BOT_TOKEN`
- Убедитесь, что токен не содержит лишних пробелов

### Приложение не открывается в Telegram
- Проверьте, что URL в Menu Button правильный
- Убедитесь, что используете HTTPS (не HTTP)
- Проверьте, что сервер доступен извне (ngrok/деплой)

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в терминале
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все переменные окружения заполнены
4. Проверьте, что Google Sheet имеет правильную структуру

## ✅ Checklist

- [ ] Node.js установлен (v18+)
- [ ] Зависимости установлены (frontend + backend)
- [ ] Google Cloud Project создан
- [ ] Service Account создан и JSON ключ скачан
- [ ] Google Sheet создана с 3 листами
- [ ] Service Account добавлен в Sheet с правами Editor
- [ ] Telegram Bot создан через @BotFather
- [ ] Admin ID получен через @userinfobot
- [ ] `.env` файлы созданы и заполнены
- [ ] Backend запущен и работает
- [ ] Frontend запущен и работает
- [ ] Тестирование пройдено
- [ ] Деплой выполнен (опционально)
