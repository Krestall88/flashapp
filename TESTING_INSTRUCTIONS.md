# Инструкции по тестированию исправлений

## ✅ Что было исправлено

### 1. Таблица клиентов
- ✅ Создан лист `clients` в Google Sheets
- ✅ Автоматическое сохранение клиентов при создании заказа

### 2. Итоговая сумма в заказах
- ✅ Добавлен блок с общей суммой по статусам

### 3. Исправление галереи
- ✅ Исправлено сравнение `serviceId` (приведение к строке)
- ✅ Добавлено подробное логирование

### 4. Логирование для диагностики
- ✅ Логи в браузере (консоль)
- ✅ Логи на сервере (терминал)

---

## 🔍 Как протестировать

### Шаг 1: Перезагрузите приложение

1. **Обновите страницу в браузере** (Ctrl+F5 или Cmd+Shift+R)
2. **Откройте консоль браузера** (F12 → вкладка Console)
3. **Откройте терминал где запущен сервер** (должен быть запущен `npm run dev`)

### Шаг 2: Проверьте услуги в админке

1. **Откройте админ-панель → вкладка "Услуги"**
2. **Проверьте консоль браузера**, должны появиться логи:
   ```
   ServiceManager: Loaded services: [...]
   ```
3. **Проверьте терминал сервера**, должны появиться логи:
   ```
   📥 API: GET /api/services
   🔍 Loading services from Google Sheets...
   📊 Total rows in services sheet: X
   Service SRV-1: active=true, filtered=true
   ✅ Loaded services: X
   📤 API: Returning X services
   ```

**Если услуги НЕ отображаются:**
- Скопируйте ВСЕ логи из консоли браузера
- Скопируйте ВСЕ логи из терминала сервера
- Отправьте мне оба набора логов

### Шаг 3: Проверьте галерею

1. **Нажмите на иконку 🖼️ у любой услуги**
2. **Проверьте консоль браузера**, должны появиться логи:
   ```
   Gallery: Loading images for serviceId: 3
   Gallery: Loaded images: [...]
   ```
3. **Проверьте терминал сервера**, должны появиться логи:
   ```
   📥 API: GET /api/gallery?serviceId=3
   🔍 Loading gallery from Google Sheets for serviceId: 3
   📊 Total rows in gallery sheet: X
   🔍 Filtering gallery for serviceId: 3
   Image IMG-001: serviceId="SRV-3" (SRV-3) vs "3" (3), match=false
   Image IMG-002: serviceId="3" (3) vs "3" (3), match=true
   ✅ Loaded gallery images: X
   📤 API: Returning X gallery images
   ```

**Если галерея НЕ показывает фото:**
- Скопируйте ВСЕ логи из консоли браузера
- Скопируйте ВСЕ логи из терминала сервера
- Отправьте мне оба набора логов

### Шаг 4: Проверьте итоговую сумму

1. **Откройте админ-панель → вкладка "Заказы"**
2. **Под фильтрами статусов должен быть блок с:**
   - Итоговой суммой (большими цифрами)
   - Количеством заказов
3. **Переключите фильтр** (Все → Новые → В работе → Завершенные)
4. **Сумма должна меняться** в зависимости от выбранного статуса

---

## 📋 Что нужно отправить мне

### Если услуги не отображаются:

**Из консоли браузера (F12):**
```
ServiceManager: Loaded services: ...
(все сообщения связанные с ServiceManager)
```

**Из терминала сервера:**
```
📥 API: GET /api/services
🔍 Loading services from Google Sheets...
📊 Total rows in services sheet: ...
(все сообщения связанные с services)
```

### Если галерея не показывает фото:

**Из консоли браузера (F12):**
```
Gallery: Loading images for serviceId: ...
Gallery: Loaded images: ...
```

**Из терминала сервера:**
```
📥 API: GET /api/gallery?serviceId=...
🔍 Loading gallery from Google Sheets for serviceId: ...
📊 Total rows in gallery sheet: ...
Image IMG-XXX: serviceId="..." (...) vs "..." (...), match=...
✅ Loaded gallery images: ...
```

---

## 🔧 Возможные проблемы и решения

### Проблема: Услуги не отображаются

**Логи покажут:**
- Сколько строк в таблице `services`
- Какие услуги фильтруются (active=true/false)
- Сколько услуг возвращается

**Возможные причины:**
1. Поле `active` не равно `"true"` (должно быть текстом)
2. Таблица `services` пустая
3. Все услуги имеют `active=false`

### Проблема: Галерея пустая

**Логи покажут:**
- Сколько строк в таблице `gallery`
- Какие `serviceId` сравниваются
- Почему фото не проходят фильтр

**Возможные причины:**
1. `serviceId` в таблице `gallery` не совпадает с ID услуги
   - Например: в `services` ID = `"SRV-3"`, а в `gallery` serviceId = `"3"`
   - Или наоборот: в `services` ID = `"3"`, а в `gallery` serviceId = `"SRV-3"`
2. Таблица `gallery` пустая
3. Ссылки на фото недоступны

---

## 📊 Проверка Google Sheets

### Откройте вашу таблицу и проверьте:

**Лист `services`:**
- Есть ли строки с данными?
- Поле `active` = `"true"` (текст, не TRUE, не 1)?
- Поле `id` заполнено?

**Лист `gallery`:**
- Есть ли строки с данными?
- Поле `serviceId` совпадает с `id` из листа `services`?
- Поле `imageUrl` содержит рабочие ссылки?

**Лист `clients` (новый):**
- Должен быть создан автоматически
- Будет заполняться при создании заказов

---

## 🚀 Следующие шаги

1. **Протестируйте** по инструкции выше
2. **Соберите логи** из консоли браузера и терминала сервера
3. **Отправьте мне логи** если что-то не работает
4. **Я исправлю** оставшиеся проблемы на основе логов
5. **Задеплоим** готовое решение

---

**Жду результаты тестирования и логи!** 🔍
