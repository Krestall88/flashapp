# 🎭 Демо-режим приложения

## ✅ Что реализовано:

### 1. **Принудительная темная тема** 🌙
**Проблема:** Фон приложения зависел от темы Telegram, при светлой теме выглядело плохо

**Решение:**
- Принудительно установлен черный фон `#000000` независимо от настроек Telegram
- Изменены файлы:
  - `src/index.css` - добавлен `background: #000000 !important`
  - `src/components/Layout.tsx` - фон установлен на `#000000`
  - `src/App.tsx` - цвета Telegram WebApp установлены на черный

**Результат:** Приложение всегда отображается с темной темой

---

### 2. **Админка видна всем пользователям** 👥
**Проблема:** Админка была доступна только администраторам

**Решение (демо-режим):**
- Вкладка "Админ" теперь видна всем пользователям
- Все могут просматривать:
  - ✅ Заказы
  - ✅ Услуги
  - ✅ Клиентов
  - ✅ Администраторов
- Изменения могут вносить только реальные администраторы

**Файлы:**
- `src/components/Layout.tsx` - вкладка "Админ" всегда в навигации
- `src/App.tsx` - добавлен комментарий о демо-режиме

---

### 3. **Ограничения для не-администраторов** 🔒
**Что заблокировано для обычных пользователей:**
- ❌ Редактирование заказов
- ❌ Изменение статуса заказов
- ❌ Удаление заказов
- ❌ Добавление/удаление услуг
- ❌ Добавление/удаление администраторов

**Что доступно всем:**
- ✅ Просмотр всех данных
- ✅ Переход в ЛС с клиентами
- ✅ Создание заказов (через основное приложение)

**Как работает:**
При нажатии на заблокированную кнопку появляется alert: "Только для администраторов"

**Файлы:**
- `src/components/views/AdminDashboard.tsx` - добавлены проверки `isAdmin` для кнопок

---

### 4. **Упрощенное первое сообщение бота** 📱
**Проблема:** Две кнопки "Открыть приложение" и "Админка" открывали одно и то же

**Решение:**
- Удалена кнопка "📊 Админка" из первого сообщения
- Осталась только кнопка "🚀 Открыть приложение"
- Админка доступна через вкладку внутри приложения

**Файл:**
- `server/index.js` - удалена вторая кнопка из inline_keyboard

---

## 🔐 Кто является администратором:

### В демо-режиме:
1. **Локальная разработка** (`localhost`) - всегда админ
2. **Пользователи из env** - `VITE_ADMIN_ID` в `.env`
3. **Пользователи из Google Sheets** - таблица `admins`

### Проверка прав:
```typescript
// Реальные админы могут вносить изменения
const isRealAdmin = adminId && user.id.toString() === adminId

// Все видят админку, но только реальные админы могут изменять
if (isAdmin) {
  // Разрешены изменения
} else {
  // Только просмотр
  alert('Только для администраторов')
}
```

---

## 🚀 Для продакшена (позже):

### Когда будете устанавливать для конкретных клиентов:

1. **Отключить демо-режим:**
   ```typescript
   // В src/components/Layout.tsx
   const navItems = [
     { id: 'home', label: 'Главная', icon: Home },
     { id: 'orders', label: 'Заказы', icon: ClipboardList },
     { id: 'ai', label: 'AI помощник', icon: MessageCircle },
     ...(isAdmin ? [{ id: 'admin', label: 'Админ', icon: Shield }] : []), // Вернуть условие
   ]
   ```

2. **Убрать комментарий о демо-режиме:**
   ```typescript
   // В src/App.tsx - удалить строку:
   console.log('🎭 Demo mode: Admin panel visible to all users (read-only for non-admins)')
   ```

3. **Настроить администраторов:**
   - Добавить в `.env`: `VITE_ADMIN_ID=реальный_telegram_id`
   - Или добавить в Google Sheets таблицу `admins`

---

## 📊 Текущая структура:

### Навигация (для всех):
- 🏠 Главная
- 📋 Заказы (мои заказы)
- 💬 AI помощник
- 🛡️ Админ (демо-режим)

### Админка (просмотр для всех, изменения только для админов):
- **Заказы** - список всех заказов с фильтрами
- **Услуги** - список услуг с ценами
- **Админы** - список администраторов
- **Клиенты** - список клиентов с историей заказов

---

## ✅ Что работает:

- ✅ Темная тема независимо от настроек Telegram
- ✅ Админка видна всем пользователям
- ✅ Кнопки изменений заблокированы для не-админов
- ✅ Одна кнопка в первом сообщении бота
- ✅ Все данные доступны для просмотра
- ✅ Переход в ЛС с клиентами работает
- ✅ Создание заказов работает

---

## 🧪 Как протестировать:

1. **Откройте приложение:**
   ```
   http://localhost:3000
   ```

2. **Проверьте темную тему:**
   - Фон должен быть черным независимо от темы Telegram

3. **Проверьте админку:**
   - Вкладка "Админ" должна быть видна
   - Откройте любую вкладку (Заказы, Услуги, Клиенты)
   - Попробуйте нажать на кнопки изменения
   - Должен появиться alert "Только для администраторов"

4. **Проверьте бота:**
   - Отправьте `/start` боту
   - Должна быть только одна кнопка "🚀 Открыть приложение"

---

**Готово! Приложение в демо-режиме! 🎉**
