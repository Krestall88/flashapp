# 🔧 Настройка переменных окружения для работы с настройками компании

## Проблема: Ошибка 404 при сохранении настроек

Если вы видите ошибку **"Request failed with status code 404"** при сохранении настроек компании, это означает, что клиент не может найти API endpoint `/api/settings`.

### Причина
Клиент использует переменную `VITE_API_URL` для определения адреса API. Если эта переменная не настроена или настроена неправильно, запросы идут не туда.

---

## ✅ Решение

### Для локальной разработки

1. **Убедитесь, что в корневом `.env` файле есть:**
   ```env
   VITE_API_URL=http://localhost:5000
   ```

2. **Перезапустите dev-сервер:**
   ```bash
   npm run dev
   ```

### Для Vercel деплоя

1. **Откройте Vercel Dashboard → Settings → Environment Variables**

2. **Добавьте переменную:**
   ```
   Name: VITE_API_URL
   Value: https://ваш-домен.vercel.app
   ```
   
   Замените `ваш-домен.vercel.app` на ваш реальный домен Vercel.

3. **Redeploy проект:**
   ```bash
   vercel --prod
   ```

---

## 🔍 Проверка

### В браузере (консоль):
После открытия приложения вы должны увидеть:
```
🌐 API URL: http://localhost:5000
```
или
```
🌐 API URL: https://ваш-домен.vercel.app
```

### Проверка запросов:
1. Откройте DevTools (F12) → Network
2. Попробуйте сохранить настройки
3. Найдите запрос к `/api/settings`
4. Проверьте URL запроса - он должен быть правильным

---

## 🚨 Важно

**Для Vercel:** Переменные окружения с префиксом `VITE_` должны быть настроены в Vercel Dashboard, а не только в локальном `.env` файле.

**После изменения переменных окружения в Vercel:**
- Обязательно сделайте redeploy
- Очистите кэш браузера (Ctrl+Shift+R)

---

## 📝 Текущая структура .env файлов

### Корневой `.env` (для клиента):
```env
VITE_API_URL=http://localhost:5000
VITE_ADMIN_ID=ваш_telegram_id
```

### `server/.env` (для сервера):
```env
BOT_TOKEN=ваш_токен_бота
ADMIN_ID=ваш_telegram_id
GOOGLE_SHEET_ID=id_таблицы
GOOGLE_SERVICE_ACCOUNT_EMAIL=email@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
PORT=5000
```

---

## 🔧 Быстрое исправление

Если настройки не сохраняются:

1. **Проверьте консоль браузера** - какой URL используется для запросов?
2. **Проверьте `.env` файл** - есть ли `VITE_API_URL`?
3. **Перезапустите dev-сервер** - `npm run dev`
4. **Очистите кэш браузера** - Ctrl+Shift+R

Если проблема на Vercel:
1. **Проверьте Environment Variables** в Vercel Dashboard
2. **Добавьте `VITE_API_URL`** если его нет
3. **Redeploy** проект

---

**После настройки переменных окружения все должно работать! 🚀**
