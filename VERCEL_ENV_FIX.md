# 🔧 Исправление ошибки сохранения настроек на Vercel

## Проблема
- ❌ Настройки не сохраняются
- ❌ Информация о компании не выводится
- ❌ Ошибка "Network Error" при сохранении

## Причина
**Не настроена переменная окружения `VITE_API_URL` в Vercel.**

Клиент использует дефолтный `http://localhost:5000`, который не работает в продакшне.

---

## ✅ Решение (пошагово)

### Шаг 1: Откройте Vercel Dashboard

1. Перейдите на https://vercel.com/dashboard
2. Выберите проект **flashapp**
3. Перейдите в **Settings** → **Environment Variables**

### Шаг 2: Добавьте переменные окружения

Добавьте следующие переменные для **Production**, **Preview** и **Development**:

#### 1. VITE_API_URL
```
Name: VITE_API_URL
Value: https://flashapp-beta.vercel.app
```
**ВАЖНО:** Замените `flashapp-beta.vercel.app` на ваш реальный домен Vercel.

#### 2. VITE_ADMIN_ID
```
Name: VITE_ADMIN_ID
Value: ваш_telegram_id
```

#### 3. BOT_TOKEN (если еще не добавлен)
```
Name: BOT_TOKEN
Value: ваш_новый_токен_бота
```
**ВАЖНО:** Используйте НОВЫЙ токен, так как старый был скомпрометирован.

#### 4. GOOGLE_SHEET_ID
```
Name: GOOGLE_SHEET_ID
Value: ваш_id_таблицы
```

#### 5. GOOGLE_SERVICE_ACCOUNT_EMAIL
```
Name: GOOGLE_SERVICE_ACCOUNT_EMAIL
Value: your-service-account@project.iam.gserviceaccount.com
```

#### 6. GOOGLE_PRIVATE_KEY
```
Name: GOOGLE_PRIVATE_KEY
Value: ваш_приватный_ключ_с_переносами_строк
```
**ВАЖНО:** Ключ должен включать `-----BEGIN PRIVATE KEY-----` и `-----END PRIVATE KEY-----`

#### 7. WEB_APP_URL
```
Name: WEB_APP_URL
Value: https://flashapp-beta.vercel.app
```

---

### Шаг 3: Redeploy проект

После добавления всех переменных:

1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **⋯** (три точки) → **Redeploy**
4. Выберите **Use existing Build Cache** (можно снять галочку для полной пересборки)
5. Нажмите **Redeploy**

---

### Шаг 4: Проверьте работу

После завершения деплоя:

1. **Откройте приложение** (жесткий рефреш: `Ctrl+Shift+R`)
2. **Откройте консоль браузера** (F12)
3. Должно быть сообщение:
   ```
   🌐 API URL: https://flashapp-beta.vercel.app
   ```
   
4. **Проверьте главный экран:**
   - Должна отображаться информация о компании
   
5. **Проверьте админку → Настройки:**
   - Измените любое поле
   - Нажмите "Сохранить"
   - Должно появиться "Настройки сохранены!"

---

## 🔍 Диагностика (если не работает)

### В консоли браузера (F12):

1. **Проверьте API URL:**
   ```
   Должно быть: 🌐 API URL: https://flashapp-beta.vercel.app
   НЕ должно быть: 🌐 API URL: http://localhost:5000
   ```

2. **Проверьте Network (вкладка Network):**
   - Найдите запрос `GET /api/settings`
   - Статус должен быть **200**
   - Response должен содержать JSON с настройками

3. **При сохранении настроек:**
   - Должен быть запрос `OPTIONS /api/settings` → **200**
   - Затем `PUT /api/settings` → **200**
   - Response должен содержать сохраненные данные

---

## 🚨 Частые ошибки

### 1. API URL все еще localhost
**Причина:** Не сделан redeploy после добавления переменных.
**Решение:** Redeploy проект в Vercel.

### 2. 401 Unauthorized
**Причина:** Неправильные Google Sheets credentials.
**Решение:** Проверьте `GOOGLE_SERVICE_ACCOUNT_EMAIL` и `GOOGLE_PRIVATE_KEY`.

### 3. Network Error
**Причина:** CORS или неправильный URL.
**Решение:** 
- Проверьте что `VITE_API_URL` настроен правильно
- Сделайте redeploy

### 4. Данные не сохраняются
**Причина:** Таблица settings не создана или неправильные заголовки.
**Решение:** Запустите локально:
```bash
node server/scripts/fix-settings-headers.js
```

---

## 📝 Проверка переменных окружения

Чтобы убедиться, что переменные применились:

1. В Vercel Dashboard → Settings → Environment Variables
2. Убедитесь, что все переменные есть для **Production**
3. Проверьте, что `VITE_API_URL` = `https://ваш-домен.vercel.app`

---

## ✅ Чеклист

- [ ] Добавлены все переменные окружения в Vercel
- [ ] `VITE_API_URL` = `https://flashapp-beta.vercel.app` (ваш домен)
- [ ] Сделан redeploy проекта
- [ ] Очищен кэш браузера (Ctrl+Shift+R)
- [ ] В консоли браузера правильный API URL
- [ ] Информация о компании отображается на главном экране
- [ ] Настройки сохраняются без ошибок

---

**После выполнения всех шагов приложение должно работать корректно! 🚀**
