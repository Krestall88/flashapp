# 🌐 Настройка ngrok для тестирования Telegram Mini App

## Проблема с Menu Button

Если при открытии Mini App через бота показывается **другая игра** (например, Kanji Boxing), это означает, что **Menu Button настроен неправильно** или указывает на старый URL.

---

## ✅ Правильная настройка ngrok

### Шаг 1: Запустите приложение локально

```bash
npm run dev
```

Должно запуститься:
- **Frontend**: `http://localhost:3000`
- **Backend**: `http://localhost:5000`

### Шаг 2: Запустите ngrok для frontend

```bash
ngrok http 3000
```

**Важно:** Указывайте порт **3000** (frontend), а не 5000!

### Шаг 3: Скопируйте HTTPS URL

Из вывода ngrok скопируйте URL:
```
Forwarding    https://abc123.ngrok-free.dev -> http://localhost:3000
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
              Скопируйте этот URL
```

### Шаг 4: Настройте Menu Button в BotFather

**ВАЖНО: Нужно настроить именно Menu Button, а не Web App!**

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте: `/mybots`
3. Выберите вашего бота
4. Нажмите **"Bot Settings"**
5. Нажмите **"Menu Button"**
6. Нажмите **"Edit Menu Button URL"**
7. Вставьте ngrok URL: `https://abc123.ngrok-free.dev`
8. Нажмите **"Configure Menu Button"**
9. Введите текст кнопки: `Открыть приложение`

### Шаг 5: Обновите WEB_APP_URL в backend

В `server/.env`:
```env
WEB_APP_URL=https://abc123.ngrok-free.dev
```

**Перезапустите backend:**
```bash
# Остановите npm run dev (Ctrl+C)
npm run dev
```

### Шаг 6: Откройте Mini App

1. Найдите вашего бота в Telegram
2. Нажмите кнопку меню (≡) внизу
3. Нажмите **"Открыть приложение"**
4. Mini App должен открыться с вашим UI

---

## ❌ Частые ошибки

### Ошибка 1: Белый экран

**Причины:**
- ngrok указывает на неправильный порт
- Frontend не запущен
- CORS ошибки

**Решение:**
```bash
# Убедитесь, что ngrok указывает на порт 3000
ngrok http 3000

# Проверьте, что frontend работает
curl http://localhost:3000
```

### Ошибка 2: 403 Forbidden от ngrok

**Причина:** Telegram блокирует некоторые ngrok домены

**Решение 1: Используйте другой регион**
```bash
ngrok http 3000 --region us
```

**Решение 2: Используйте ngrok с авторизацией**
```bash
# Зарегистрируйтесь на ngrok.com
# Получите authtoken
ngrok config add-authtoken YOUR_TOKEN
ngrok http 3000
```

**Решение 3: Используйте localtunnel**
```bash
npm install -g localtunnel
lt --port 3000
```

### Ошибка 3: Показывается другая игра

**Причина:** Menu Button указывает на старый URL или чужое приложение

**Решение:**
1. Откройте BotFather
2. `/mybots` → Ваш бот → Bot Settings → Menu Button
3. **"Edit Menu Button URL"**
4. Вставьте **новый** ngrok URL
5. Сохраните

**Проверка:**
После сохранения BotFather должен показать:
```
Menu button URL: https://abc123.ngrok-free.dev
```

### Ошибка 4: Backend не доступен

**Причина:** `VITE_API_URL` указывает на localhost, который недоступен из Telegram

**Решение для локального тестирования:**

**Вариант 1: Запустите второй ngrok для backend**
```bash
# Терминал 1
npm run dev

# Терминал 2
ngrok http 3000

# Терминал 3
ngrok http 5000
```

Затем в `.env`:
```env
VITE_API_URL=https://backend-url.ngrok-free.dev
```

**Вариант 2: Используйте production backend**
```env
VITE_API_URL=https://your-app.up.railway.app
```

---

## 🔍 Проверка настроек

### Проверьте Menu Button

1. BotFather → `/mybots` → Ваш бот → Bot Settings → Menu Button
2. Должно быть: `https://abc123.ngrok-free.dev`
3. **НЕ должно быть**: старый URL, localhost, или чужой домен

### Проверьте ngrok

```bash
# Откройте http://127.0.0.1:4040
# Там вы увидите все запросы к вашему приложению
```

### Проверьте переменные окружения

**Frontend (`.env`):**
```env
VITE_API_URL=http://localhost:5000
# или
VITE_API_URL=https://backend.ngrok-free.dev
VITE_ADMIN_ID=323976163
```

**Backend (`server/.env`):**
```env
WEB_APP_URL=https://abc123.ngrok-free.dev
TELEGRAM_BOT_TOKEN=7741841139:AAFEXYsVbzpcEioW0p9ai0qURcynT3Ub0bI
ADMIN_ID=323976163
```

---

## 🎯 Полный процесс тестирования

### 1. Запустите приложение
```bash
npm run dev
```

### 2. Запустите ngrok
```bash
ngrok http 3000
```

### 3. Обновите Menu Button в BotFather
```
https://your-unique-id.ngrok-free.dev
```

### 4. Обновите WEB_APP_URL в server/.env
```env
WEB_APP_URL=https://your-unique-id.ngrok-free.dev
```

### 5. Перезапустите backend
```bash
# Ctrl+C
npm run dev
```

### 6. Откройте бота в Telegram
- Нажмите кнопку меню (≡)
- Нажмите "Открыть приложение"
- Должен открыться ваш UI

### 7. Создайте тестовый заказ
- Выберите услугу
- Заполните форму
- Отправьте заявку
- Должно появиться подтверждение
- Админу должно прийти уведомление

---

## 💡 Советы

### Используйте ngrok с фиксированным доменом

**Бесплатный план ngrok:**
- URL меняется при каждом запуске
- Нужно каждый раз обновлять BotFather

**Платный план ngrok ($8/месяц):**
- Фиксированный домен
- Настраиваете один раз

### Альтернатива: Деплой на Vercel

Вместо ngrok можно сразу деплоить на Vercel:

```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой
vercel

# Получите URL
# Настройте в BotFather
```

**Преимущества:**
- Постоянный URL
- Быстрая работа
- Бесплатно

---

## 🚀 Рекомендация

**Для разработки:**
- Используйте ngrok для быстрого тестирования
- Или деплойте на Vercel для постоянного URL

**Для production:**
- Frontend → Vercel
- Backend → Railway
- Настройте Menu Button на Vercel URL

---

## ❓ FAQ

### Нужно ли каждый раз обновлять Menu Button?

**Да**, если используете бесплатный ngrok (URL меняется).

**Нет**, если:
- Используете платный ngrok с фиксированным доменом
- Деплоите на Vercel (постоянный URL)

### Можно ли тестировать без ngrok?

**Нет**, Telegram Mini Apps работают только через HTTPS.

Варианты:
- ngrok (временный HTTPS)
- localtunnel (временный HTTPS)
- Vercel (постоянный HTTPS)

### Почему показывается чужая игра?

Menu Button указывает на старый URL или чужое приложение.

**Решение:**
1. BotFather → Menu Button → Edit URL
2. Вставьте **ваш** ngrok URL
3. Сохраните

### Как проверить, что Menu Button настроен правильно?

1. BotFather → `/mybots` → Ваш бот → Bot Settings → Menu Button
2. Должен быть ваш ngrok URL
3. Откройте этот URL в браузере - должен открыться ваш UI

---

Готово! Теперь вы можете тестировать Mini App через Telegram. 🎉
