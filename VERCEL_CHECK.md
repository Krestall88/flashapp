# 🔍 Проверка настроек Vercel

## Проблема
Заказ не создается в production, но работает локально.

## Причина
Скорее всего неправильно настроен `VITE_API_URL` на Vercel.

---

## ✅ Как проверить и исправить

### Шаг 1: Проверьте переменные окружения на Vercel

1. Откройте [vercel.com](https://vercel.com)
2. Ваш проект → **Settings** → **Environment Variables**
3. Проверьте переменную **`VITE_API_URL`**

**Должно быть:**
```
VITE_API_URL=https://your-backend.up.railway.app
```

**НЕ должно быть:**
- `http://localhost:5000` ❌
- Пустое значение ❌
- Неправильный URL ❌

### Шаг 2: Получите правильный URL Railway

1. Откройте [railway.app](https://railway.app)
2. Ваш проект → **Settings** → **Domains**
3. Скопируйте URL (например: `https://telegram-detailing-production.up.railway.app`)

### Шаг 3: Обновите переменную на Vercel

1. Vercel → Settings → Environment Variables
2. Найдите `VITE_API_URL`
3. Нажмите **Edit**
4. Вставьте правильный Railway URL
5. Нажмите **Save**

### Шаг 4: Redeploy

После изменения переменных нужно передеплоить:

1. Vercel → Deployments
2. Последний деплой → три точки → **Redeploy**
3. Или просто сделайте `git push` - автоматически задеплоится

---

## 🔍 Как проверить что URL правильный

### Проверка 1: Откройте Railway URL в браузере

```
https://your-backend.up.railway.app/api/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "timestamp": "2026-01-30T..."
}
```

### Проверка 2: Проверьте в DevTools

1. Откройте Mini App в браузере (не в Telegram)
2. F12 → Console
3. Попробуйте создать заказ
4. Посмотрите в Console - должен быть лог:
```
Creating order with data: {...}
API URL: https://your-backend.up.railway.app
```

Если видите `http://localhost:5000` - значит переменная не применилась.

---

## 🚨 Частые ошибки

### Ошибка 1: Забыли префикс VITE_

❌ Неправильно:
```
API_URL=https://...
```

✅ Правильно:
```
VITE_API_URL=https://...
```

**Vite требует префикс `VITE_` для всех переменных!**

### Ошибка 2: Не сделали Redeploy

После изменения переменных нужно **обязательно** передеплоить проект.

### Ошибка 3: Неправильный URL Railway

Проверьте что URL:
- Начинается с `https://` (не `http://`)
- Заканчивается на `.up.railway.app`
- Без `/` в конце
- Без `/api` в конце

---

## 📝 Правильная конфигурация

### Vercel Environment Variables

```env
VITE_API_URL=https://telegram-detailing-production.up.railway.app
VITE_ADMIN_ID=323976163
```

### Railway Environment Variables

```env
PORT=5000
TELEGRAM_BOT_TOKEN=7741841139:AAFEXYsVbzpcEioW0p9ai0qURcynT3Ub0bI
ADMIN_ID=323976163
WEB_APP_URL=https://telegram-detailing-app.vercel.app
GOOGLE_SHEET_ID=19cLDSm-zbuZho-ZKovJcUN_xGBoMnZRxbCyu7aUNaKM
GOOGLE_SERVICE_ACCOUNT_EMAIL=telegram-detailing-bot@telegram-detailing.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
```

---

## 🎯 Быстрая проверка

Выполните эти команды в браузере на странице Mini App:

```javascript
// Откройте Console (F12)
console.log('API URL:', import.meta.env.VITE_API_URL)
console.log('Admin ID:', import.meta.env.VITE_ADMIN_ID)
```

Должно показать правильные значения.

---

## ✅ После исправления

1. Проверьте что переменная правильная
2. Сделайте Redeploy на Vercel
3. Подождите 1-2 минуты
4. Откройте Mini App в Telegram
5. Попробуйте создать заказ
6. Должно работать! ✅

---

## 🆘 Если все еще не работает

Откройте Mini App в **Telegram Desktop**:
1. Ctrl+Shift+I (откроется DevTools)
2. Console → посмотрите на ошибки
3. Network → посмотрите на запрос к `/api/orders`
4. Скопируйте ошибку и отправьте мне

Или проверьте логи Railway:
1. Railway → Deployments → Latest
2. Посмотрите логи - есть ли ошибки при создании заказа
